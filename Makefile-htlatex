default:	index.html

publish:	snapshot.png

index.html:     index.tex
	make -C FIGs
	make -C FIGs
	htlatex index.tex "../slides.cfg, ../math.cfg, 2, charset=utf-8" " -cunihtf"

snapshot.png:	index.html
	phantomjs ../rasterize.js http://localhost/$(notdir $(CURDIR))/index.html snapshot.png 400px*300px
